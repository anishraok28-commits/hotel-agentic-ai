# Frontend environment variables (docs/api-contract.md section 6.1)
# VITE_* variables are the ONLY env vars the frontend may read. No secrets.

VITE_ENV=local
VITE_API_BASE_URL=https://api.example.com